# Force OpenGL to avoid the Vulkan/Wayland HDR bug
vo=gpu
gpu-api=opengl

# Disable HDR probing (the "hint" that's causing the crash)
target-colorspace-hint=no

# Use Intel's native hardware decoding
hwdec=vaapi

video-aspect-override=16:10

